/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/symmetrical-computing-machine',
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig
